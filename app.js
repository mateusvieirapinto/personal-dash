// Adiciona a classe "active" ao botão menu-h quando clicado com o mouse:
document.querySelector(".menu-h").addEventListener("click", () => {
  document.querySelector(".menu-nav").classList.toggle("active");
});
// Fazendo com que a classe menu-h ganhe uma classe adicional "active" trazendo novos atributos visuais quando o botão for clicado.
